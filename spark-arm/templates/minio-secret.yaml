apiVersion: v1
kind: Secret
metadata:
  name: {{ include "spark-arm.fullname" . }}-minio
  labels:
    {{- include "spark-arm.labels" . | nindent 4 }}
    app.kubernetes.io/component: minio
type: Opaque
stringData:
  access-key: {{ .Values.minio.credentials.accessKey | quote }}
  secret-key: {{ .Values.minio.credentials.secretKey | quote }}
  s3a-access-key: {{ .Values.minio.credentials.accessKey | quote }}
  s3a-secret-key: {{ .Values.minio.credentials.secretKey | quote }} 