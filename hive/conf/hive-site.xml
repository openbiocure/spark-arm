<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>
<configuration>
    <!-- Hive Metastore Configuration -->
    <property>
        <name>javax.jdo.option.ConnectionURL</name>
        <value>jdbc:postgresql://${HIVE_METASTORE_DB_HOST:-postgresql}:${HIVE_METASTORE_DB_PORT:-5432}/${HIVE_METASTORE_DB_NAME:-hive}</value>
        <description>PostgreSQL connection URL</description>
    </property>
    <property>
        <name>javax.jdo.option.ConnectionDriverName</name>
        <value>org.postgresql.Driver</value>
        <description>PostgreSQL JDBC driver</description>
    </property>
    <property>
        <name>javax.jdo.option.ConnectionUserName</name>
        <value>${HIVE_METASTORE_DB_USER:-hive}</value>
        <description>PostgreSQL username</description>
    </property>
    <property>
        <name>javax.jdo.option.ConnectionPassword</name>
        <value>${HIVE_METASTORE_DB_PASSWORD:-hive}</value>
        <description>PostgreSQL password</description>
    </property>

    <!-- Hive Server2 Configuration -->
    <property>
        <name>hive.server2.thrift.port</name>
        <value>${HIVE_SERVER2_PORT:-10000}</value>
        <description>Port number of HiveServer2 Thrift interface</description>
    </property>
    <property>
        <name>hive.server2.thrift.bind.host</name>
        <value>${HIVE_SERVER2_BIND_HOST:-0.0.0.0}</value>
        <description>Bind host on which to run the HiveServer2 Thrift interface</description>
    </property>

    <!-- Hive Metastore Configuration -->
    <property>
        <name>hive.metastore.uris</name>
        <value>thrift://${HIVE_METASTORE_HOST:-localhost}:${HIVE_METASTORE_PORT:-9083}</value>
        <description>Thrift URI for the remote metastore</description>
    </property>
    <property>
        <name>hive.metastore.schema.verification</name>
        <value>false</value>
        <description>Disable schema verification</description>
    </property>

    <!-- Hive Warehouse Configuration -->
    <property>
        <name>hive.metastore.warehouse.dir</name>
        <value>${HIVE_WAREHOUSE_DIR:-/user/hive/warehouse}</value>
        <description>Location of default database for the warehouse</description>
    </property>
    <property>
        <name>hive.exec.scratchdir</name>
        <value>${HIVE_SCRATCH_DIR:-/tmp/hive}</value>
        <description>Scratch space for Hive jobs</description>
    </property>
</configuration> 